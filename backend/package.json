{
  "name": "puma",
  "version": "1.0.0",
  "description": "code challenge",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "migrate": "npx prisma migrate deploy && npx prisma generate",
    "dev": "docker compose up db -d && nodemon --watch 'src' --exec 'node src/index.js'",
    "migrate:test": "DATABASE_PORT=5433 npm run migrate",
    "test": "DATABASE_PORT=5433 node --experimental-vm-modules --no-warnings node_modules/jest/bin/jest.js --silent",
    "start": "NODE_ENV=production node src/index.js",
    "setup": "chmod +x scripts/setup.sh && ./scripts/setup.sh"
  },
  "author": "mateusvrs",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.21.1",
    "cors": "^2.8.5",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.7",
    "prisma": "^5.21.1",
    "supertest": "^7.0.0"
  }
}